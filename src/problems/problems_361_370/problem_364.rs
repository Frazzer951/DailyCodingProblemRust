// NOT DONE

/* MEDIUM
Describe an algorithm to compute the longest increasing subsequence of an array
of numbers in O(n log n) time.
*/

fn problem_364() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore]
    fn test_problem_364() {
        assert_eq!(problem_364(), 1);
    }
}
