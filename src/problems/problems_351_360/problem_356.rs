// NOT DONE

/* HARD
Implement a queue using a set of fixed-length arrays.

The queue should support enqueue, dequeue, and get_size operations.
*/

fn problem_356() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore]
    fn test_problem_356() {
        assert_eq!(problem_356(), 1);
    }
}
