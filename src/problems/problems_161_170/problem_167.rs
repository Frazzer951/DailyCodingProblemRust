// NOT DONE

/* HARD
Given a list of words, find all pairs of unique indices such that the
concatenation of the two words is a palindrome.

For example, given the list ["code", "edoc", "da", "d"], return [(0, 1), (1, 0),
(2, 3)].
*/

fn problem_167() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_problem_167() {
        assert_eq!(problem_167(), 0);
    }
}
