// NOT DONE

/* HARD
Given a list, sort it using this method: reverse(lst, i, j), which reverses lst 
from i to j.
*/

fn problem_147() {}

#[cfg(test)]
mod tests {
    #[test]
    fn test_problem_147() {
        assert_eq!(2 + 2, 4);
    }
}