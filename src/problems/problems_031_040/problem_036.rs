// NOT DONE

/* MEDIUM
Given the root to a binary search tree, find the second largest node in the
tree.
*/

fn problem_036() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore]
    fn test_problem_036() {
        assert_eq!(problem_036(), 1);
    }
}
