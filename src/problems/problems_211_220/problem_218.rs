// NOT DONE

/* MEDIUM
Write an algorithm that computes the reversal of a directed graph. For example,
if a graph consists of A -> B -> C, it should become A <- B <- C.
*/

fn problem_218() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_problem_218() {
        assert_eq!(problem_218(), 0);
    }
}
