// NOT DONE

/* EASY
Given a set of points (x, y) on a 2D cartesian plane, find the two closest
points. For example, given the points [(1, 1), (-1, -1), (3, 4), (6, 1), (-1,
-6), (-4, -3)], return [(-1, -1), (1, 1)].
*/

fn problem_340() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore]
    fn test_problem_340() {
        assert_eq!(problem_340(), 1);
    }
}
