// NOT DONE

/* MEDIUM
Given a list of integers and a number K, return which contiguous elements of the
list sum to K.

For example, if the list is [1, 2, 3, 4, 5] and K is 9, then it should return
[2, 3, 4], since 2 + 3 + 4 = 9.
*/

fn problem_102() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore]
    fn test_problem_102() {
        assert_eq!(problem_102(), 1);
    }
}
