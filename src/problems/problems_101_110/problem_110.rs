// NOT DONE

/* MEDIUM
Given a binary tree, return all paths from the root to leaves.

For example, given the tree:

   1
  / \
 2   3
    / \
   4   5


Return [[1, 2], [1, 3, 4], [1, 3, 5]].
*/

fn problem_110() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_problem_110() {
        assert_eq!(problem_110(), 0);
    }
}
