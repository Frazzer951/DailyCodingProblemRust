// NOT DONE

/* MEDIUM
Given an integer n and a list of integers l, write a function that randomly
generates a number from 0 to n-1 that isn't in l (uniform).
*/

fn problem_090() -> i64 {
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_problem_090() {
        assert_eq!(problem_090(), 1);
    }
}
